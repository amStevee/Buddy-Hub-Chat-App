name: CI

on:
  push:
    branches: [main]

  jobs:
    build:
      runs-on: ubuntu-latest

      steps:
        - uses: actions/checkout@v3

        - name: Install depenencies
          run: pnpm Install

        - name: Run build
          run: pnpm run build
